* recipe tags
    ** Full stack test of recipe tags in create view
        *** tag select handling is wrong
            ****It shouldn't create it should add because the tags should already exist
        *** make sure tags aren't duplicating the db
    ** Need to be able to add more tag create forms in recipe form template
    ** Add tags in recipe detail view
    ** Add tags in recipe update view
* can't line break in directions

----MVP LINE------------------------------------------

* recipe update ugly errors on empty ingredient name
* ingredient name not editable on edit template
